一种合并操作，将前景叠加到背景颜色之上，但假设两者在共同覆盖的半透明区域中不存在重叠。

**Disjoint Over** 节点会根据前景和背景输入的 Alpha 通道执行两种不同的混合方式之一。

* 当 f + b ≤ 1 时，输出的 RGB 分量为：`F + B`
* 当 f + b > 1时，输出的 RGB 分量为：`(F + b(1 − f)) / b`

输出的 Alpha 分量始终为 f + b 与 1 中的较小值：`min(f + b, 1)`。
## **参数说明**

* **Foreground**：Color 4 类型的前景输入，其中 `F` 表示该参数的 RGB 分量，`f` 表示该参数的 Alpha 分量。
* **Background**：Color 4 类型的背景输入，其中 `B` 表示该参数的 RGB 分量，`b` 表示该参数的 Alpha 分量。
* **Mix**：混合操作的权重。**Mix** 值越高，混合效果越明显。默认值为 `1`。取值超出 `0` 到 `1` 范围时，会产生超出该节点预期功能之外的未定义效果。

## **节点使用说明**
下面是一个简单的节点图示例，展示了如何使用 **Disjoint Over** 节点混合两种纹理。

下图展示了两张原始图像和混合后的材质。

<strong>Foreground</strong>

<strong>Background</strong>

混合后的材质如下：


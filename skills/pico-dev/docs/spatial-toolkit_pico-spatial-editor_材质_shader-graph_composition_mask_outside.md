将遮罩的补值乘到输入的所有通道上

**Outside** 节点会将 `1 − 遮罩` 乘到输入的所有通道上。从视觉效果上看，这相当于保留遮罩之外的部分。
## **参数说明**

* **In**：应用遮罩的输入值。
* **Mask**：用于与输入值相乘的值。

## 节点使用说明
下面的节点图展示了如何把遮罩应用到指定材质。

下图展示了遮罩的补值应用到砖墙材质的效果。

<strong>In</strong>

<strong>Mask</strong>

混合后的材质如下：


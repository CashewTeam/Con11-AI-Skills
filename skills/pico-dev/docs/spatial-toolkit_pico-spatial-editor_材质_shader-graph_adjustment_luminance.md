接收一个 RGB 颜色作为输入，并输出一个在所有颜色通道中均包含该颜色亮度信息的灰度值。

## 参数说明

* **In**: 待处理的输入颜色。
* **Luma Coefficients**: 定义了计算亮度时所依据的颜色空间的权重系数。不同的颜色空间对 R、G、B 三个通道的贡献度有不同的定义。可选的值为 **acescg**、**rec2020/rec2100** 或 **rec709** 颜色空间的亮度系数。默认值为 **acescg** 的亮度系数，即 `(0.2722287, 0.6740818, 0.0536895)`。

## 节点使用说明
该节点通过计算输入颜色向量与亮度系数（Luma Coefficients）的点积来得到最终的灰度值。
以下是一个简单的节点图示例，展示了如何使用 **Luminance** 节点（**Type** 为 **Color 3**）将一张图片转换为灰度图：

应用 Shader Graph 前后的对比效果如下：

应用 Shader Graph 前

应用 Shader Graph 后


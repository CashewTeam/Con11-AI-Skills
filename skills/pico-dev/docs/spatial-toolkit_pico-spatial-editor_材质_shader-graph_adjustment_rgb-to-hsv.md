将颜色从 RGB 颜色空间转换到 HSV 颜色空间。

## 参数说明

* **In**: 输入一个标准 RGB 格式的颜色。
* **Out**: 输出一个包含色相 (Hue)、饱和度 (Saturation) 和明度 (Value) 的颜色向量。

## 节点使用说明
此节点执行标准的颜色空间转换算法，将笛卡尔坐标系下的 RGB 值转换为圆柱坐标系下的 HSV 值，便于进行更符合直觉的颜色调整，例如单独修改色相或饱和度。

# createSession | PICO Spatial SDK

spatialml:securemr / com.pico.spatial.ml.securemr / SpatialMLInstance / createSession 
# createSession
```kotlin
abstract fun createSession(config: SpatialMLSession.InitInfo): SpatialMLSession?
```
Create a  SpatialMLSession  from this instance. 
#### Return
The created session, if the instance is ready. Otherwise, null.
# COLORS | PICO Spatial SDK

spatialml:securemr / com.pico.spatial.ml.securemr / Tensor / TensorUsage / COLORS 
# COLORS
```kotlin
COLORS
```
A color array, the data in the tensor will be interpreted as an array of 

```
template <typename T>struct { T red; T green; T blue; T alpha = 1;};
```
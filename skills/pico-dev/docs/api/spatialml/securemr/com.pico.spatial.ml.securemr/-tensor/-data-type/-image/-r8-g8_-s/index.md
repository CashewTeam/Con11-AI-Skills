# R8G8_S | PICO Spatial SDK

spatialml:securemr / com.pico.spatial.ml.securemr / Tensor / DataType / Image / R8G8_S 
# R8G8_S
```kotlin
R8G8_S
```
16-bit pixel, interpreted as two 8-bit signed integer value (8 bits for RED, 8 bits for GREEN).
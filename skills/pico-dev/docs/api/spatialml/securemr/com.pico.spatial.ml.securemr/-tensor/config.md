# config | PICO Spatial SDK

spatialml:securemr / com.pico.spatial.ml.securemr / Tensor / config 
# config
```kotlin
val config: Tensor.InitInfo
```
#### Parameters
config 
Configuration at initialization.
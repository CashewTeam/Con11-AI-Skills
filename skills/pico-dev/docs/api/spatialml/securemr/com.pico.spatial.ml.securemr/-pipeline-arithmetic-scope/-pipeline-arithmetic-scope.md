# PipelineArithmeticScope | PICO Spatial SDK

spatialml:securemr / com.pico.spatial.ml.securemr / PipelineArithmeticScope / PipelineArithmeticScope 
# PipelineArithmeticScope
```kotlin
constructor()
```
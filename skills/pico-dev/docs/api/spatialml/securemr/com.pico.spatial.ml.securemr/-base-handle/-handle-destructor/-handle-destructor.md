# HandleDestructor | PICO Spatial SDK

spatialml:securemr / com.pico.spatial.ml.securemr / BaseHandle / HandleDestructor / HandleDestructor 
# HandleDestructor
```kotlin
constructor(target: BaseHandle)
```
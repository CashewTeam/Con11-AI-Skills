# applyChange | PICO Spatial SDK

spatialml:readback / com.pico.spatial.ml.readback / TensorContent / applyChange 
# applyChange
```kotlin
fun applyChange()
```
Write the content back to reset the tensor. This method will use the data current held by this  TensorContent  to override the tensor's content. 
#### Throws
Spatial MLException 
if the reset fails.
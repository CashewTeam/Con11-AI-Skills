# close | PICO Spatial SDK

spatialml:readback / com.pico.spatial.ml.readback / TensorContent / close 
# close
```kotlin
open override fun close()
```
Unmap the buffer and close the shared memory. Do not call any methods after close it.
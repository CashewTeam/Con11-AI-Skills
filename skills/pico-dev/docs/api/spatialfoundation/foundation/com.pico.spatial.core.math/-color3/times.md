# times | PICO Spatial SDK

foundation / com.pico.spatial.core.math / Color3 / times 
# times
```kotlin
operator fun times(scalar: Float): Color3
```
Multiplies the  Color3  instance by a scalar value. 
#### Return
A new  Color3  instance, which is the result of multiplying the  Color3  instance by the scalar value. 
#### Parameters
scalar 
The scalar value to multiply the  Color3  instance by. 
```kotlin
operator fun times(another: Color3): Color3
```
Multiplies the  Color3  instance by another  Color3  instance. 
#### Return
A new  Color3  instance, which is the result of multiplying the  Color3  instance by the another  Color3  instance. 
#### Parameters
another 
Another  Color3  instance.
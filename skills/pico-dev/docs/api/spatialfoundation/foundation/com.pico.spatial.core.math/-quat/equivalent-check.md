# equivalentCheck | PICO Spatial SDK

foundation / com.pico.spatial.core.math / Quat / equivalentCheck 
# equivalentCheck
```kotlin
fun equivalentCheck(other: Quat): Boolean
```
Determine whether two rotation operations are equivalent. 
#### Return
true if two rotation operations are equivalent, false otherwise. 
#### Parameters
other 
The other  Quat  to compare with.
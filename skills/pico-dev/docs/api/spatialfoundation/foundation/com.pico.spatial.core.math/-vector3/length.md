# length | PICO Spatial SDK

foundation / com.pico.spatial.core.math / Vector3 / length 
# length
```kotlin
fun length(): Float
```
Gets the length of the  Vector3  instance. 
#### Return
The length of the  Vector3  instance.
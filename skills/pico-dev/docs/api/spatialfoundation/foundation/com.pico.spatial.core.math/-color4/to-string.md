# toString | PICO Spatial SDK

foundation / com.pico.spatial.core.math / Color4 / toString 
# toString
```kotlin
open override fun toString(): String
```
Returns a string representation of the  Color4  object. 
#### Return
A string representation of the  Color4  object.
# quaternion | PICO Spatial SDK

foundation / com.pico.spatial.core.math / Rotation / quaternion 
# quaternion
```kotlin
val quaternion: Quat
```
The internal unit quaternion that represents this rotation. 
It is initialized by normalizing the input  quat  to ensure it always represents a pure rotation and maintains the class's invariant.
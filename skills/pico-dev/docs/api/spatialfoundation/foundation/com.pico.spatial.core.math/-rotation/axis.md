# axis | PICO Spatial SDK

foundation / com.pico.spatial.core.math / Rotation / axis 
# axis
```kotlin
val axis: Vector3
```
The normalized axis of rotation. For an identity rotation (zero angle), a default axis (e.g., Z-axis) may be returned.
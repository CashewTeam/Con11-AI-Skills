# equivalentCheck | PICO Spatial SDK

foundation / com.pico.spatial.core.math / EulerAngles / equivalentCheck 
# equivalentCheck
```kotlin
fun equivalentCheck(other: EulerAngles): Boolean
```
Determines whether two rotation operations are equivalent. 
#### Return
true  if two rotation operations are equivalent;  false  otherwise.
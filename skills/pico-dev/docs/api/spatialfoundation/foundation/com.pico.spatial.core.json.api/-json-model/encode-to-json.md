# encodeToJson | PICO Spatial SDK

foundation / com.pico.spatial.core.json.api / JsonModel / encodeToJson 
# encodeToJson
```kotlin
open override fun encodeToJson(): JSONObject
```
Encode current object to JSONObject. 
#### Return
The encoded JSONObject.
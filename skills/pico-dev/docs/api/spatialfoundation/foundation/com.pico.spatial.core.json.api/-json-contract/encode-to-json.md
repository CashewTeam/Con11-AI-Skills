# encodeToJson | PICO Spatial SDK

foundation / com.pico.spatial.core.json.api / JsonContract / encodeToJson 
# encodeToJson
```kotlin
abstract fun encodeToJson(): JSONObject
```
Encode current object to JSONObject. 
#### Return
The encoded JSONObject.
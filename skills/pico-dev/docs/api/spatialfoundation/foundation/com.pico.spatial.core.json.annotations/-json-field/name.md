# name | PICO Spatial SDK

foundation / com.pico.spatial.core.json.annotations / JsonField / name 
# name
```kotlin
val name: String
```
Specifies the name of the JSON field. If left empty, the field name will be used by default. 
#### Return
The custom name of the JSON field.
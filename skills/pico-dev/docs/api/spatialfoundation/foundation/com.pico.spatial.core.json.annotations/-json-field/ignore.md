# ignore | PICO Spatial SDK

foundation / com.pico.spatial.core.json.annotations / JsonField / ignore 
# ignore
```kotlin
val ignore: Boolean = false
```
Indicates whether this field should be ignored during serialization or deserialization. 
#### Return
true  if the field should be ignored;  false  otherwise.
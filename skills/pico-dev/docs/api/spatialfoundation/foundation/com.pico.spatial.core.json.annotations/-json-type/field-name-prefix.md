# fieldNamePrefix | PICO Spatial SDK

foundation / com.pico.spatial.core.json.annotations / JsonType / fieldNamePrefix 
# fieldNamePrefix
```kotlin
val fieldNamePrefix: String
```
Prefix to add to each JSON field name. 
#### Return
The prefix string. The default value is empty ( "" ).
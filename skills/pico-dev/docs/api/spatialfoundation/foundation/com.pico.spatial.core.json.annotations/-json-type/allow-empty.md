# allowEmpty | PICO Spatial SDK

foundation / com.pico.spatial.core.json.annotations / JsonType / allowEmpty 
# allowEmpty
```kotlin
val allowEmpty: Boolean = true
```
Whether fields are allowed to be empty. 
#### Return
true  if empty fields are allowed;  false  otherwise. The default value is  true .
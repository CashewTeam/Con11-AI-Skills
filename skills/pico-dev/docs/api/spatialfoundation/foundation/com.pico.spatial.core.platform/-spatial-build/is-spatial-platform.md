# isSpatialPlatform | PICO Spatial SDK

foundation / com.pico.spatial.core.platform / SpatialBuild / isSpatialPlatform 
# isSpatialPlatform
```kotlin
@JvmStatic
```fun  isSpatialPlatform ( ) :  Boolean 
Checks whether the current running OS supports PICO OS spatial features. 
#### Return
true  if the current running OS is PICO OS;  false  otherwise.
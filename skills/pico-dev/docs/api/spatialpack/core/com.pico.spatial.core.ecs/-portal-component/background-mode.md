# backgroundMode | PICO Spatial SDK

core / com.pico.spatial.core.ecs / PortalComponent / backgroundMode 
# backgroundMode
```kotlin
@ExperimentalSpatialApi
```var  backgroundMode :  PortalBackgroundMode 
The background mode of the portal. The default value is  PortalBackgroundMode.SOLID_COLOR . 
#### Throws
Illegal State Exception 
If this method is called when the experimental API is not enabled.
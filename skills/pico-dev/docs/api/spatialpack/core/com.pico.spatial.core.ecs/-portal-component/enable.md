# enable | PICO Spatial SDK

core / com.pico.spatial.core.ecs / PortalComponent / enable 
# enable
```kotlin
var enable: Boolean
```
The enabled state of the  PortalComponent . The default value is  true , indicating enabled.
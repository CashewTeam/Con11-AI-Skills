# distanceAttenuationMode | PICO Spatial SDK

core / com.pico.spatial.core.ecs / ObjectAudioComponent / distanceAttenuationMode 
# distanceAttenuationMode
```kotlin
var distanceAttenuationMode: DistanceAttenuationMode
```
The distance attenuation mode for audio.
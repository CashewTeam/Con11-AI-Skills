# reverbVolume | PICO Spatial SDK

core / com.pico.spatial.core.ecs / ObjectAudioComponent / reverbVolume 
# reverbVolume
```kotlin
var reverbVolume: Float
```
The reverb volume level of the  ObjectAudioComponent . The valid value range is  [0.0, 1.0] . The default value is  1.0f . 
This property only takes effect when VST (Video SeeThrough) is enabled.
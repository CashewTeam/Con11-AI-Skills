# getAnimationResources | PICO Spatial SDK

core / com.pico.spatial.core.ecs / Entity / getAnimationResources 
# getAnimationResources
```kotlin
@MainThread
```fun  getAnimationResources ( ) :  Array < AnimationResource > 
Gets the animation resources associated with the current entity. 
#### Return
An array of  AnimationResource  objects associated with the current entity.
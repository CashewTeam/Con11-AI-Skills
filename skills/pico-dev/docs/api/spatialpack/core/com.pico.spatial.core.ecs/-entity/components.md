# components | PICO Spatial SDK

core / com.pico.spatial.core.ecs / Entity / components 
# components
```kotlin
val components: Entity.ComponentSet
```
A set of components that have been added to this entity.
# getChildrenCount | PICO Spatial SDK

core / com.pico.spatial.core.ecs / Entity / getChildrenCount 
# getChildrenCount
```kotlin
@MainThread
```fun  getChildrenCount ( ) :  Int 
Gets the number of child entities of the current entity. 
#### Return
The number of child entities. A value less than  0  indicates the retrieval failure.
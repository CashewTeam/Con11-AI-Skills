# removeAllChildren | PICO Spatial SDK

core / com.pico.spatial.core.ecs / Entity / removeAllChildren 
# removeAllChildren
```kotlin
@MainThread
```fun  removeAllChildren ( ) :  Boolean 
Removes all child entities from the current entity. 
#### Return
true  if the removal succeeds;  false  otherwise.
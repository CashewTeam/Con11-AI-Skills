# CloneOptions | PICO Spatial SDK

core / com.pico.spatial.core.ecs / Entity / CloneOptions / CloneOptions 
# CloneOptions
```kotlin
constructor(recursive: Boolean = false, shouldShareMaterialInstance: Boolean = false)
```
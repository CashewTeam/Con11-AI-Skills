# getChildren | PICO Spatial SDK

core / com.pico.spatial.core.ecs / Entity / getChildren 
# getChildren
```kotlin
@MainThread
```fun  getChildren ( ) :  Array < Entity > 
Gets all child entities of the current entity. 
#### Return
An array of child entities.
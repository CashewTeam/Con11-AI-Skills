# Entity | PICO Spatial SDK

core / com.pico.spatial.core.ecs / Entity / Entity 
# Entity
```kotlin
@MainThread
```constructor ( ) 
The default constructor for  Entity . 
The entity's initial  enabled  state is  true .
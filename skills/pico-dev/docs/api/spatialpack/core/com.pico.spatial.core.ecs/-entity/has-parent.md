# hasParent | PICO Spatial SDK

core / com.pico.spatial.core.ecs / Entity / hasParent 
# hasParent
```kotlin
@MainThread
```fun  hasParent ( ) :  Boolean 
Checks if the current entity has a parent entity. 
#### Return
true  if the current entity has a parent entity;  false  otherwise.
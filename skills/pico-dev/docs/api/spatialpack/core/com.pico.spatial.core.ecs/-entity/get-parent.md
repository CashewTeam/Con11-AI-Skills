# getParent | PICO Spatial SDK

core / com.pico.spatial.core.ecs / Entity / getParent 
# getParent
```kotlin
@MainThread
```fun  getParent ( ) :  Entity ? 
Gets the parent entity of the current entity. 
#### Return
The parent entity if it exists, or null if there is no parent.
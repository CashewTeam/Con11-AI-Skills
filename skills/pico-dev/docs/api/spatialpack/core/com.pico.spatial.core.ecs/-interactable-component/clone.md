# clone | PICO Spatial SDK

core / com.pico.spatial.core.ecs / InteractableComponent / clone 
# clone
```kotlin
open override fun clone(): Component
```
Creates and returns a new instance cloned from the current object. 
#### Return
A cloned instance of the current object.
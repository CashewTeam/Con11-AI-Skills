# resource | PICO Spatial SDK

core / com.pico.spatial.core.ecs / ImageBasedLightSource / Single / resource 
# resource
```kotlin
val resource: TextureResource
```
#### Parameters
resource 
The HDR environment  TextureResource  (typically a cubemap). Currently supported format:  .ktx .
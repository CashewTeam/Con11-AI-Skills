# blendPercentage | PICO Spatial SDK

core / com.pico.spatial.core.ecs / ImageBasedLightSource / Blend / blendPercentage 
# blendPercentage
```kotlin
val blendPercentage: Float
```
#### Parameters
blend Percentage 
The blend ratio, where  0.0  = 100% first texture,  1.0  = 100% second texture. The default value is  0.5  (equal blend).
# receivesShadowEnabled | PICO Spatial SDK

core / com.pico.spatial.core.ecs / GroundShadowComponent / receivesShadowEnabled 
# receivesShadowEnabled
```kotlin
var receivesShadowEnabled: Boolean
```
Whether the entity receives ground shadow.
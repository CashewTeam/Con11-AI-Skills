# scale | PICO Spatial SDK

core / com.pico.spatial.core.ecs / EnvironmentLightingSettingsComponent / scale 
# scale
```kotlin
var scale: Float
```
Scale factor for environment IBL intensity. 
Range: [0, +∞).  0.0  disables environment IBL; values > 0 scale intensity proportionally.
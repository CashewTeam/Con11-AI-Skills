# removeMixerGroup | PICO Spatial SDK

core / com.pico.spatial.core.ecs / AudioMixerGroupsComponent / removeMixerGroup 
# removeMixerGroup
```kotlin
fun removeMixerGroup(name: String)
```
Removes an  AudioMixerGroupResource  by its name. If not found, no action is taken. 
#### Parameters
name 
The name of the  AudioMixerGroupResource  to remove. The maximum length is 256 characters.
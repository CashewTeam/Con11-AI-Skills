# getMixerGroup | PICO Spatial SDK

core / com.pico.spatial.core.ecs / AudioMixerGroupsComponent / getMixerGroup 
# getMixerGroup
```kotlin
fun getMixerGroup(name: String): AudioMixerGroupResource?
```
Gets an  AudioMixerGroupResource  by its name. 
#### Return
The  AudioMixerGroupResource  if found; otherwise  null . 
#### Parameters
name 
The resource name. The maximum length is 256 characters.
# addMixerGroup | PICO Spatial SDK

core / com.pico.spatial.core.ecs / AudioMixerGroupsComponent / addMixerGroup 
# addMixerGroup
```kotlin
fun addMixerGroup(mixerGroupResource: AudioMixerGroupResource)
```
Adds an  AudioMixerGroupResource  to this component. 
#### Parameters
mixer Group Resource 
The  AudioMixerGroupResource  to add.
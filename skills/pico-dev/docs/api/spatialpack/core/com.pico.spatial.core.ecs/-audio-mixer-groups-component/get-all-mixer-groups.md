# getAllMixerGroups | PICO Spatial SDK

core / com.pico.spatial.core.ecs / AudioMixerGroupsComponent / getAllMixerGroups 
# getAllMixerGroups
```kotlin
fun getAllMixerGroups(): List<AudioMixerGroupResource>
```
Gets all  AudioMixerGroupResource  in this component. 
#### Return
A list of  AudioMixerGroupResource .
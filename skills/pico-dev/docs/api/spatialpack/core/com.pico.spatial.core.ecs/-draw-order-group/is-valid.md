# isValid | PICO Spatial SDK

core / com.pico.spatial.core.ecs / DrawOrderGroup / isValid 
# isValid
```kotlin
fun isValid(): Boolean
```
Checks if the draw order group is valid. 
#### Return
True if the draw order group is valid, false otherwise.
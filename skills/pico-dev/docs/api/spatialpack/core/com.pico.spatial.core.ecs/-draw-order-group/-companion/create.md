# create | PICO Spatial SDK

core / com.pico.spatial.core.ecs / DrawOrderGroup / Companion / create 
# create
```kotlin
@JvmStatic
```fun  create ( ) :  DrawOrderGroup 
Creates a new draw order group. 
#### Return
The created draw order group.
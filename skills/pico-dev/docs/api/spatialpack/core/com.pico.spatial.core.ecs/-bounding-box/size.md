# size | PICO Spatial SDK

core / com.pico.spatial.core.ecs / BoundingBox / size 
# size
```kotlin
val size: Vector3
```
The size of the bounding box, represented as a vector. Precision error: 0.00001F.
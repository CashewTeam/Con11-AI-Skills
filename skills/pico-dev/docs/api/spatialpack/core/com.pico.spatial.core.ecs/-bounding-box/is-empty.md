# isEmpty | PICO Spatial SDK

core / com.pico.spatial.core.ecs / BoundingBox / isEmpty 
# isEmpty
```kotlin
fun isEmpty(): Boolean
```
Checks if the bounding box is empty. 
#### Return
true  if empty;  false  otherwise.
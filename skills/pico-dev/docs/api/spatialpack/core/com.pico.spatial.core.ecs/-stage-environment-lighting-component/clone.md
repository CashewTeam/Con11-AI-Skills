# clone | PICO Spatial SDK

core / com.pico.spatial.core.ecs / StageEnvironmentLightingComponent / clone 
# clone
```kotlin
open override fun clone(): Component
```
Creates and returns a new instance of the current object. 
#### Return
An instance cloned from the current object.
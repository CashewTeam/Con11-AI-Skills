# intensityExponent | PICO Spatial SDK

core / com.pico.spatial.core.ecs / StageEnvironmentLightingComponent / intensityExponent 
# intensityExponent
```kotlin
var intensityExponent: Float
```
The intensity exponent for the current image-based lighting. The intensity is 2^intensityExponent.
# isCloneable | PICO Spatial SDK

core / com.pico.spatial.core.ecs / Component / Companion / isCloneable 
# isCloneable
```kotlin
@JvmStatic
```fun  isCloneable ( componentClass :  Class < out  Component > ) :  Boolean 
Checks if the component class is cloneable. 
#### Return
true  if the component class is cloneable;  false  otherwise. 
#### Parameters
component Class 
The component class to check.
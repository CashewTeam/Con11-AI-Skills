# simulationClock | PICO Spatial SDK

core / com.pico.spatial.core.ecs / PhysicsWorldComponent / simulationClock 
# simulationClock
```kotlin
var simulationClock: SimulationClock
```
A custom clock driving the physics simulation, defaults to the engine clock.
# rotationOffset | PICO Spatial SDK

core / com.pico.spatial.core.ecs / AnchorComponent / rotationOffset 
# rotationOffset
```kotlin
var rotationOffset: Quat
```
The rotation offset of the actual object relative to its  AnchorTarget .
# isVortexEnabled | PICO Spatial SDK

core / com.pico.spatial.core.ecs / ParticleComponent / isVortexEnabled 
# isVortexEnabled
```kotlin
var isVortexEnabled: Boolean
```
Whether the vortex effect is enabled. 
The vortex effect is a force that rotates particles around a specified axis in the scene.
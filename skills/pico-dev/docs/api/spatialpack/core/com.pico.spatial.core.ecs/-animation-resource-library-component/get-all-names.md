# getAllNames | PICO Spatial SDK

core / com.pico.spatial.core.ecs / AnimationResourceLibraryComponent / getAllNames 
# getAllNames
```kotlin
fun getAllNames(): List<String>
```
Retrieves all resource names currently stored in the library. 
#### Return
A list of all animation resource names.
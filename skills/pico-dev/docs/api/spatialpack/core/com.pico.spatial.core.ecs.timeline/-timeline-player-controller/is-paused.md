# isPaused | PICO Spatial SDK

core / com.pico.spatial.core.ecs.timeline / TimelinePlayerController / isPaused 
# isPaused
```kotlin
fun isPaused(): Boolean
```
Returns whether the timeline is paused. 
#### Return
true  if paused,  false  otherwise.
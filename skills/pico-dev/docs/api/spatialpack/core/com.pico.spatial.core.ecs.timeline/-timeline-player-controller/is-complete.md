# isComplete | PICO Spatial SDK

core / com.pico.spatial.core.ecs.timeline / TimelinePlayerController / isComplete 
# isComplete
```kotlin
fun isComplete(): Boolean
```
Returns whether the timeline has completed. 
#### Return
true  if completed,  false  otherwise.
# close | PICO Spatial SDK

core / com.pico.spatial.core.ecs.timeline / TimelinePlayerController / close 
# close
```kotlin
open override fun close()
```
Closes the current controller. 
You need to release the timeline player controller manually to ensure that it no longer takes up memory.
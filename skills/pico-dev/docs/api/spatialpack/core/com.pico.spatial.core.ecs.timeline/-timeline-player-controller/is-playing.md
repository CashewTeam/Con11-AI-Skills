# isPlaying | PICO Spatial SDK

core / com.pico.spatial.core.ecs.timeline / TimelinePlayerController / isPlaying 
# isPlaying
```kotlin
fun isPlaying(): Boolean
```
Returns whether the timeline is playing. 
#### Return
true  if playing,  false  otherwise.
# isStopped | PICO Spatial SDK

core / com.pico.spatial.core.ecs.timeline / TimelinePlayerController / isStopped 
# isStopped
```kotlin
fun isStopped(): Boolean
```
Returns whether the timeline is stopped. 
#### Return
true  if stopped,  false  otherwise.
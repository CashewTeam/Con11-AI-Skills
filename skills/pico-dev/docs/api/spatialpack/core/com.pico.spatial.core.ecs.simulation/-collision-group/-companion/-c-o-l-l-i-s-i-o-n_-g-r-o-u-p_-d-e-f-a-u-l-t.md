# COLLISION_GROUP_DEFAULT | PICO Spatial SDK

core / com.pico.spatial.core.ecs.simulation / CollisionGroup / Companion / COLLISION_GROUP_DEFAULT 
# COLLISION_GROUP_DEFAULT
```kotlin
const val COLLISION_GROUP_DEFAULT: UInt
```
The default collision group (0x00000001). 
Most objects belong to this group by default. It corresponds to the first bit (bit 0) being set.
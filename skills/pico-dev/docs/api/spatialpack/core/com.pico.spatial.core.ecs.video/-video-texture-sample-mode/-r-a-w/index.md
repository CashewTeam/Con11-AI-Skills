# RAW | PICO Spatial SDK

core / com.pico.spatial.core.ecs.video / VideoTextureSampleMode / RAW 
# RAW
```kotlin
RAW
```
Raw texture mode. 
This mode will broadcast the raw texture of video component, and this texture will be sampled by the same video texture sample name of shader graph material.
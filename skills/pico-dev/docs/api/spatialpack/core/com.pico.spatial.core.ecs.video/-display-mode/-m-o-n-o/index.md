# MONO | PICO Spatial SDK

core / com.pico.spatial.core.ecs.video / DisplayMode / MONO 
# MONO
```kotlin
MONO
```
The MONO display mode,when the video is 3D video, the video will be displayed in a single view with no stereo effect.
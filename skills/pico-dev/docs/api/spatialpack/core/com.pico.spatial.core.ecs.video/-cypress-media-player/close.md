# close | PICO Spatial SDK

core / com.pico.spatial.core.ecs.video / CypressMediaPlayer / close 
# close
```kotlin
open override fun close()
```
Releases the player manually to free the resources and memory it occupies. 
Since  CypressMediaPlayer  is a singleton, you should call this method to release the player explicitly when it is no longer needed.
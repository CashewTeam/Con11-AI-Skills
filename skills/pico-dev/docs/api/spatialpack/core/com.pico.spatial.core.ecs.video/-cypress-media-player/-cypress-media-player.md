# CypressMediaPlayer | PICO Spatial SDK

core / com.pico.spatial.core.ecs.video / CypressMediaPlayer / CypressMediaPlayer 
# CypressMediaPlayer
```kotlin
constructor()
```
Constructs a new  CypressMediaPlayer  instance. 
This constructor creates a handle for a  CypressMediaPlayer . 
Ensure the player is valid before calling other APIs; otherwise, an  IllegalStateException  may be thrown.
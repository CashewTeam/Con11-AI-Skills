# onStopped | PICO Spatial SDK

core / com.pico.spatial.core.ecs.video / CypressMediaPlayerCallback / onStopped 
# onStopped
```kotlin
abstract fun onStopped()
```
Callback triggered when the video playback is stopped.
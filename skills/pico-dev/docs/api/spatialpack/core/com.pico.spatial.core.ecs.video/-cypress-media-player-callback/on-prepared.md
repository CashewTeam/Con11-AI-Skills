# onPrepared | PICO Spatial SDK

core / com.pico.spatial.core.ecs.video / CypressMediaPlayerCallback / onPrepared 
# onPrepared
```kotlin
abstract fun onPrepared()
```
Callback triggered after the user calls the  CypressMediaPlayer.prepareAsync()  method.
# onCompleted | PICO Spatial SDK

core / com.pico.spatial.core.ecs.video / CypressMediaPlayerCallback / onCompleted 
# onCompleted
```kotlin
abstract fun onCompleted()
```
Callback triggered when video playback reaches the end.
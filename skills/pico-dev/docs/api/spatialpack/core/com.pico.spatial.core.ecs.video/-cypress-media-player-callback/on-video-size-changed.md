# onVideoSizeChanged | PICO Spatial SDK

core / com.pico.spatial.core.ecs.video / CypressMediaPlayerCallback / onVideoSizeChanged 
# onVideoSizeChanged
```kotlin
abstract fun onVideoSizeChanged(width: Int, height: Int)
```
Callback triggered when the video size changes.
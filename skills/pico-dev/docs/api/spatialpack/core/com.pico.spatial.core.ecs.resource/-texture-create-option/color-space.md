# colorSpace | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / TextureCreateOption / colorSpace 
# colorSpace
```kotlin
var colorSpace: TextureColorSpace
```
The color space of the texture. Defaults to  TextureColorSpace.SRGB .
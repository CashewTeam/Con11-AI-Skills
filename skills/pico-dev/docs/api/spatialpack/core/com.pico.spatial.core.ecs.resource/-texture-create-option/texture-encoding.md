# textureEncoding | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / TextureCreateOption / textureEncoding 
# textureEncoding
```kotlin
var textureEncoding: TextureEncoding
```
The encoding of the texture. Defaults to  TextureEncoding.SRGB .
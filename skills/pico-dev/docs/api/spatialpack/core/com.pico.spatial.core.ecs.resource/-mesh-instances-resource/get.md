# get | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / MeshInstancesResource / get 
# get
```kotlin
fun get(id: String): MeshInstancesResource.Instance?
```
Gets an instance from the resource. 
#### Return
The instance with the specified ID. 
#### Parameters
id 
The ID of the instance to get.
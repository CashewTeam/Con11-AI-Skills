# triangleIndices | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / MeshModel / triangleIndices 
# triangleIndices
```kotlin
val triangleIndices: List<Int>
```
The triangle indices that define the mesh faces.
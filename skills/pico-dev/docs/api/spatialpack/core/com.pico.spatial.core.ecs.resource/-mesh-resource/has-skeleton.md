# hasSkeleton | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / MeshResource / hasSkeleton 
# hasSkeleton
```kotlin
fun hasSkeleton(): Boolean
```
Returns whether this mesh has a skeleton (i.e., is a skinned mesh). 
#### Return
true  if the mesh has a skeleton;  false  otherwise.
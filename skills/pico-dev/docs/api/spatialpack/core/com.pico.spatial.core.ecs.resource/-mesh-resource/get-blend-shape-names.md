# getBlendShapeNames | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / MeshResource / getBlendShapeNames 
# getBlendShapeNames
```kotlin
fun getBlendShapeNames(): Array<String>
```
Gets the names of all blend shapes in the mesh. 
#### Return
An array of strings, each representing the name of a blend shape.
# READ_ONLY | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / ResourceLoadingException / ResourceErrorCode / READ_ONLY 
# READ_ONLY
```kotlin
READ_ONLY
```
The resource is read-only and cannot be modified.
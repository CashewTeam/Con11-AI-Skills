# NOT_DIR | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / ResourceLoadingException / ResourceErrorCode / NOT_DIR 
# NOT_DIR
```kotlin
NOT_DIR
```
The resource is not a directory, but a directory was expected.
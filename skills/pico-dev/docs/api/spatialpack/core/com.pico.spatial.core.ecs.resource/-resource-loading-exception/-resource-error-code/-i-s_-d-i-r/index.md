# IS_DIR | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / ResourceLoadingException / ResourceErrorCode / IS_DIR 
# IS_DIR
```kotlin
IS_DIR
```
The resource is a directory, but a non-directory was expected.
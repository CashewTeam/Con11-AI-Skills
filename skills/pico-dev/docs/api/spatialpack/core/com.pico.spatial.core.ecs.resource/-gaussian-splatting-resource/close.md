# close | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / GaussianSplattingResource / close 
# close
```kotlin
open override fun close()
```
You need to manually release the resource to free the memory it occupies.
# repeat | PICO Spatial SDK

core / com.pico.spatial.core.ecs.resource / AnimationResource / repeat 
# repeat
```kotlin
fun repeat(count: Int): AnimationResource
```
Creates a new animation resource with a specified repeat count. 
#### Return
The new animation resource. 
#### Parameters
count 
The number of times the animation should repeat. 
#### Throws
Illegal State Exception 
If this resource is closed or invalid.
# playbackController | PICO Spatial SDK

core / com.pico.spatial.core.ecs.event / AudioEvents / PlaybackStopped / playbackController 
# playbackController
```kotlin
val playbackController: AudioPlayerController
```
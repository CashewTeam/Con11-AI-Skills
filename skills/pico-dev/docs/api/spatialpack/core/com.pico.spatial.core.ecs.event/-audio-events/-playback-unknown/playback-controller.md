# playbackController | PICO Spatial SDK

core / com.pico.spatial.core.ecs.event / AudioEvents / PlaybackUnknown / playbackController 
# playbackController
```kotlin
val playbackController: AudioPlayerController
```
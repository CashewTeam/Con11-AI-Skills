# playbackController | PICO Spatial SDK

core / com.pico.spatial.core.ecs.event / AudioEvents / PlaybackPaused / playbackController 
# playbackController
```kotlin
val playbackController: AudioPlayerController
```
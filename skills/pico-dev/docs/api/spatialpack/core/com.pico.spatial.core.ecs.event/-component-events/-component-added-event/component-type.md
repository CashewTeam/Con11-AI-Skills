# componentType | PICO Spatial SDK

core / com.pico.spatial.core.ecs.event / ComponentEvents / ComponentAddedEvent / componentType 
# componentType
```kotlin
val componentType: Class<out Component>
```
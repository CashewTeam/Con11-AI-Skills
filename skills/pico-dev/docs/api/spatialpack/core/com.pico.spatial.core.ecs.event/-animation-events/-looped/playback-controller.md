# playbackController | PICO Spatial SDK

core / com.pico.spatial.core.ecs.event / AnimationEvents / Looped / playbackController 
# playbackController
```kotlin
val playbackController: AnimationPlaybackController
```
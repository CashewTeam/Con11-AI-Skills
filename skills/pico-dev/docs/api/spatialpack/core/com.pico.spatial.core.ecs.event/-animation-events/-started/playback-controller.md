# playbackController | PICO Spatial SDK

core / com.pico.spatial.core.ecs.event / AnimationEvents / Started / playbackController 
# playbackController
```kotlin
val playbackController: AnimationPlaybackController
```
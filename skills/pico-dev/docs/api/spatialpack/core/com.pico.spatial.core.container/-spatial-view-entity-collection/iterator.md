# iterator | PICO Spatial SDK

core / com.pico.spatial.core.container / SpatialViewEntityCollection / iterator 
# iterator
```kotlin
open operator override fun iterator(): Iterator<Entity>
```
Gets an  Iterator  of the  SpatialViewEntityCollection . 
#### Return
An  Iterator  of the  SpatialViewEntityCollection .
# SpatialViewEntityCollection | PICO Spatial SDK

core / com.pico.spatial.core.container / SpatialViewEntityCollection / SpatialViewEntityCollection 
# SpatialViewEntityCollection
```kotlin
constructor()
```
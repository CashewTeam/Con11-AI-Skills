# isEmpty | PICO Spatial SDK

core / com.pico.spatial.core.container / SpatialViewEntityCollection / isEmpty 
# isEmpty
```kotlin
open override fun isEmpty(): Boolean
```
Checks if a  SpatialViewEntityCollection  is empty. 
#### Return
true  if the  SpatialViewEntityCollection  is empty.
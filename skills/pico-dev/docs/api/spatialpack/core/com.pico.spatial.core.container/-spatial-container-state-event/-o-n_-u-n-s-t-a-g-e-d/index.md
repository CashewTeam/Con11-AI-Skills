# ON_UNSTAGED | PICO Spatial SDK

core / com.pico.spatial.core.container / SpatialContainerStateEvent / ON_UNSTAGED 
# ON_UNSTAGED
```kotlin
ON_UNSTAGED
```
Event indicates that the  SpatialContainer  becomes unstaged (is no longer fully visible or is occluded).
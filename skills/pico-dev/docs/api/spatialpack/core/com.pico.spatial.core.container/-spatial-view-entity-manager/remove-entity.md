# removeEntity | PICO Spatial SDK

core / com.pico.spatial.core.container / SpatialViewEntityManager / removeEntity 
# removeEntity
```kotlin
abstract fun removeEntity(entity: Entity)
```
Remove  Entity  from this content, if present.
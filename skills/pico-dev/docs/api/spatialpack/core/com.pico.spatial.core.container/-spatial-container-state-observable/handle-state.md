# handleState | PICO Spatial SDK

core / com.pico.spatial.core.container / SpatialContainerStateObservable / handleState 
# handleState
```kotlin
open fun handleState(state: SpatialContainerState)
```
Override this function to get the new  SpatialContainerState  when it changes. 
#### Parameters
state 
The new  SpatialContainerState .
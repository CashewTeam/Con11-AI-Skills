# addObserver | PICO Spatial SDK

core / com.pico.spatial.core.container / SpatialContainerStateObservable / addObserver 
# addObserver
```kotlin
fun addObserver(observer: SpatialContainerStateObserver)
```
Add an observer for a spatial container's state.
# blendLayer | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / AnimationPlayConfig / blendLayer 
# blendLayer
```kotlin
val blendLayer: Int
```
The blend layer value to use for playing the animation. 
The default value is -1, which means the animation will be played on the default blend layer.
# transitionMode | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / AnimationPlayConfig / transitionMode 
# transitionMode
```kotlin
val transitionMode: AnimationTransitionMode
```
The transition mode value to use for playing the animation. 
The default behavior is that for playing skeletal animations, the CROSSFADE mode will be used, while for other animations, the COMPOSE mode will be used.
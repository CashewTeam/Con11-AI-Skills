# SkeletonJoint | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / SkeletonJoint 
# SkeletonJoint
```kotlin
class SkeletonJoint
```
Data of a joint in a skeleton. 
Members 
## Properties
index 
```kotlin
val index: Int
```
The index of the joint, same as the index of array. 
name 
```kotlin
val name: String
```
The name of the joint, must be unique within its skeleton. 
parent Index 
```kotlin
val parentIndex: Int
```
The parent joint of index.
# additive | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / SpatialAnimation / additive 
# additive
```kotlin
var additive: Boolean
```
Whether to add this animation additively over the current one.
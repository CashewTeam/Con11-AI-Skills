# repeatMode | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / TweenAnimation / repeatMode 
# repeatMode
```kotlin
fun repeatMode(repeatMode: RepeatMode): TweenAnimation
```
Sets the repeat mode of animation. 
#### Return
This  TweenAnimation  object, for method chaining. 
#### Parameters
repeat Mode 
The repeat mode.
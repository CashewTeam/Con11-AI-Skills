# bindTarget | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / TweenAnimation / bindTarget 
# bindTarget
```kotlin
fun bindTarget(animationBindTarget: AnimationBindTarget): TweenAnimation
```
Sets the  AnimationBindTarget  of the animation. 
#### Return
This  TweenAnimation  object, for method chaining. 
#### Parameters
animation Bind Target 
The  AnimationBindTarget  of the animation.
# trimDuration | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / TweenAnimation / trimDuration 
# trimDuration
```kotlin
fun trimDuration(trimDuration: Float): TweenAnimation
```
Sets the duration of the animation to trim, in seconds. 
#### Return
This  TweenAnimation  object, for method chaining. 
#### Parameters
trim Duration 
The duration of the animation to trim, in seconds.
# trimStart | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / TweenAnimation / trimStart 
# trimStart
```kotlin
fun trimStart(trimStart: Float): TweenAnimation
```
Sets the start time (in seconds) from which to begin trimming the animation. 
#### Return
This  TweenAnimation  object, for method chaining. 
#### Parameters
trim Start 
The start time of the trimmed animation segment, in seconds.
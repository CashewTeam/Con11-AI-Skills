# isPaused | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / AnimationPlaybackController / isPaused 
# isPaused
```kotlin
fun isPaused(): Boolean
```
Returns whether the animation is paused. 
#### Return
true  if paused,  false  otherwise.
# getSpeed | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / AnimationPlaybackController / getSpeed 
# getSpeed
```kotlin
fun getSpeed(): Float
```
Gets the current playback speed of the animation. 
#### Return
The playback speed of the animation.
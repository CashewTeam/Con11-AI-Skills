# isStopped | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / AnimationPlaybackController / isStopped 
# isStopped
```kotlin
fun isStopped(): Boolean
```
Returns whether the animation is currently stopped. 
#### Return
true  if stopped,  false  otherwise.
# isComplete | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / AnimationPlaybackController / isComplete 
# isComplete
```kotlin
fun isComplete(): Boolean
```
Returns whether the animation has completed. 
#### Return
true  if completed,  false  otherwise.
# isPlaying | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / AnimationPlaybackController / isPlaying 
# isPlaying
```kotlin
fun isPlaying(): Boolean
```
Returns whether the animation is playing. 
#### Return
true  if playing,  false  otherwise.
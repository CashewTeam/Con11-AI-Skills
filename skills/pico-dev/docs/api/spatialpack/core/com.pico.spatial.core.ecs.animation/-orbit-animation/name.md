# name | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / OrbitAnimation / name 
# name
```kotlin
fun name(name: String): OrbitAnimation
```
Set the name of the animation. 
#### Return
This  OrbitAnimation  object to allow for method chaining. 
#### Parameters
name 
The name of animation, should no more than 63 ascii characters.
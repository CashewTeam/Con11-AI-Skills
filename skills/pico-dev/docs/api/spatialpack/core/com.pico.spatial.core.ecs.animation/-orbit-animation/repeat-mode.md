# repeatMode | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / OrbitAnimation / repeatMode 
# repeatMode
```kotlin
fun repeatMode(repeatMode: RepeatMode): OrbitAnimation
```
Set the repeat mode of the animation. 
#### Return
This  OrbitAnimation  object to allow for method chaining. 
#### Parameters
repeat Mode 
The repeat mode of animation.
# delay | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / OrbitAnimation / delay 
# delay
```kotlin
fun delay(delay: Float): OrbitAnimation
```
Set the delay of the animation. 
#### Return
This  OrbitAnimation  object to allow for method chaining. 
#### Parameters
delay 
The delay of animation, should be greater than 0.
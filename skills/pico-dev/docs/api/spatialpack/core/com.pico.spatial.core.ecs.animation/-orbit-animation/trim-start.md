# trimStart | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / OrbitAnimation / trimStart 
# trimStart
```kotlin
fun trimStart(trimStart: Float): OrbitAnimation
```
Set the trim start of the animation. 
#### Return
This  OrbitAnimation  object to allow for method chaining. 
#### Parameters
trim Start 
The trim start of animation, should be greater than 0.
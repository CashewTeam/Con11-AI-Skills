# trimDuration | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / OrbitAnimation / trimDuration 
# trimDuration
```kotlin
fun trimDuration(trimDuration: Float): OrbitAnimation
```
Set the trim duration of the animation. 
#### Return
This  OrbitAnimation  object to allow for method chaining. 
#### Parameters
trim Duration 
The trim duration of animation, should be greater than 0.
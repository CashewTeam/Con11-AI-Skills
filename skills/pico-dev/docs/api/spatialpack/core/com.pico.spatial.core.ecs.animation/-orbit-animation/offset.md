# offset | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / OrbitAnimation / offset 
# offset
```kotlin
fun offset(offset: Float): OrbitAnimation
```
Set the offset of the animation. 
#### Return
This  OrbitAnimation  object to allow for method chaining. 
#### Parameters
offset 
The offset of animation, should be greater than 0.
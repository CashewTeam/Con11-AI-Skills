# trimEnd | PICO Spatial SDK

core / com.pico.spatial.core.ecs.animation / OrbitAnimation / trimEnd 
# trimEnd
```kotlin
fun trimEnd(trimEnd: Float): OrbitAnimation
```
Set the trim end of the animation. 
#### Return
This  OrbitAnimation  object to allow for method chaining. 
#### Parameters
trim End 
The trim end of animation, should be greater than 0.
# BACKWARD | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioGroupResourcePlayMode / BACKWARD 
# BACKWARD
```kotlin
BACKWARD
```
Backward sequence mode. Audio resources will be played in reverse order.
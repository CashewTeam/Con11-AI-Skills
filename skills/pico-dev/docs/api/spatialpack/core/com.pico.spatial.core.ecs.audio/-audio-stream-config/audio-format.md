# audioFormat | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioStreamConfig / audioFormat 
# audioFormat
```kotlin
val audioFormat: AudioFormat?
```
PCM audio format configuration including sample rate and bit depth.
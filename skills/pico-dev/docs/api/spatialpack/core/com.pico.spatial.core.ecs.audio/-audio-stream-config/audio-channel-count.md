# audioChannelCount | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioStreamConfig / audioChannelCount 
# audioChannelCount
```kotlin
val audioChannelCount: Int
```
Number of audio channels per frame.
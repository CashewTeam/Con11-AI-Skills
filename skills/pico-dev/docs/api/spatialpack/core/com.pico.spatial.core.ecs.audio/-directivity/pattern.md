# pattern | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / Directivity / pattern 
# pattern
```kotlin
var pattern: Float
```
The pattern of audio directivity, which refers to the shape of the sound projection. It determines how focused or dispersed the sound is. 
Value range: 0.0, 1.0. The default value is  0.0f .
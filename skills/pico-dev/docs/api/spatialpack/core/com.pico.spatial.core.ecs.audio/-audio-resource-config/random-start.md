# randomStart | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioResourceConfig / randomStart 
# randomStart
```kotlin
val randomStart: Boolean
```
Whether playback should begin at a random position within the audio resource's duration.
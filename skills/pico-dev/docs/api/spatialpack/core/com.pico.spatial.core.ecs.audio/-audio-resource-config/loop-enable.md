# loopEnable | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioResourceConfig / loopEnable 
# loopEnable
```kotlin
val loopEnable: Boolean
```
Whether the audio resource should playback in loop mode.
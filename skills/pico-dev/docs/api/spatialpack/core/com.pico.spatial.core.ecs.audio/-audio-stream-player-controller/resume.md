# resume | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioStreamPlayerController / resume 
# resume
```kotlin
fun resume(): Boolean
```
Resume the pausing state audio. 
#### Return
true if resume successfully otherwise false.
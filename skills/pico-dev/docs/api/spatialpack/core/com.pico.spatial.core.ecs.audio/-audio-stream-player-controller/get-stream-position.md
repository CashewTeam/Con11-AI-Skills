# getStreamPosition | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioStreamPlayerController / getStreamPosition 
# getStreamPosition
```kotlin
fun getStreamPosition(): Long
```
Retrieves the current stream position in bytes. 
#### Return
The current stream position in bytes, or -1 if unavailable.
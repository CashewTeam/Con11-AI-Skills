# getVolume | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioStreamPlayerController / getVolume 
# getVolume
```kotlin
fun getVolume(): Float
```
Gets the playback volume. 
#### Return
float value above 0.0f if getVolume successfully otherwise -1.0f.
# valid | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioStreamPlayerController / valid 
# valid
```kotlin
@get:JvmName(name = "isValid")
```val  valid :  Boolean 
The controller is valid.
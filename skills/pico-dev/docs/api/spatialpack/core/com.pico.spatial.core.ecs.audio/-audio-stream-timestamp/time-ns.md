# timeNs | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioStreamTimestamp / timeNs 
# timeNs
```kotlin
val timeNs: Long
```
The timestamp of current audio frame data being played by controller, precision is nanoseconds.
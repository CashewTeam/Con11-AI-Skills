# interleaved | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioFormat / interleaved 
# interleaved
```kotlin
var interleaved: Boolean
```
Whether the channel data is interleaved, default is true.
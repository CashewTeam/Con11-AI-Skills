# sampleRate | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioFormat / sampleRate 
# sampleRate
```kotlin
var sampleRate: Int
```
The sample rate of the channel data, default is 48000.
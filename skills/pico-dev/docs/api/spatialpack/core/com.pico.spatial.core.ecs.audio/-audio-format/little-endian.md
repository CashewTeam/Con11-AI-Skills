# littleEndian | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioFormat / littleEndian 
# littleEndian
```kotlin
var littleEndian: Boolean
```
Whether the channel data is little endian, default is true.
# getFrameSize | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioFormat / getFrameSize 
# getFrameSize
```kotlin
fun getFrameSize(channelCnt: Int): Int
```
Get the size of a frame in bytes. 
#### Return
The size of a frame in bytes. 
#### Parameters
channel Cnt 
The number of channels.
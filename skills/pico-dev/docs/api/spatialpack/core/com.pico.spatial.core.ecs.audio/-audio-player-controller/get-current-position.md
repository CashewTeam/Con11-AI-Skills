# getCurrentPosition | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioPlayerController / getCurrentPosition 
# getCurrentPosition
```kotlin
fun getCurrentPosition(): Long
```
Gets the current playback position of the audio file in milliseconds. 
#### Return
An integer value that is greater than  0  if successful;  -1  otherwise.
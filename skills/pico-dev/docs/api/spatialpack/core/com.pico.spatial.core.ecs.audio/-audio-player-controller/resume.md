# resume | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioPlayerController / resume 
# resume
```kotlin
fun resume(): Boolean
```
Resumes a paused audio. 
#### Return
true  if the audio resumes successfully;  false  otherwise.
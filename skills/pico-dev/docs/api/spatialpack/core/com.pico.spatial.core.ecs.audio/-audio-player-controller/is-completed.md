# isCompleted | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioPlayerController / isCompleted 
# isCompleted
```kotlin
fun isCompleted(): Boolean
```
Checks if an audio is finished playing or not. 
#### Return
true  if the audio is finished playing;  false  otherwise.
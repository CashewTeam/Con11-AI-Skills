# getPlaybackSpeed | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioPlayerController / getPlaybackSpeed 
# getPlaybackSpeed
```kotlin
fun getPlaybackSpeed(): Float
```
Gets the current playback speed for the audio. 
#### Return
The playback speed as a positive float if successful;  -1.0f  otherwise.
# stop | PICO Spatial SDK

core / com.pico.spatial.core.ecs.audio / AudioPlayerController / stop 
# stop
```kotlin
fun stop(): Boolean
```
Stops an audio. 
#### Return
true  if the audio stops successfully;  false  otherwise.
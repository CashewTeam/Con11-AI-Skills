# UpdateResult | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform.containers / WindowContainerParamsUpdater / UpdateResult / UpdateResult 
# UpdateResult
```kotlin
protected constructor()
```
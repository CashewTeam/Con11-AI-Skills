# isFocused | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform.containers / SpatialContainerStateManager / isFocused 
# isFocused
```kotlin
abstract val isFocused: State<Boolean>
```
Whether the current  SpatialContainer  is focused.
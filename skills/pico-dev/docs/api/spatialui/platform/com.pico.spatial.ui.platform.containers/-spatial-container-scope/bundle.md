# bundle | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform.containers / SpatialContainerScope / bundle 
# bundle
```kotlin
abstract val bundle: Bundle?
```
Custom data that can be passed through the container scope.
# onViewpointChanged | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform / VolumeViewPointListener / onViewpointChanged 
# onViewpointChanged
```kotlin
abstract fun onViewpointChanged(viewpoint: ViewPoint)
```
Called when the viewpoint of the container changes. 
#### Parameters
viewpoint 
The viewpoint of the container.
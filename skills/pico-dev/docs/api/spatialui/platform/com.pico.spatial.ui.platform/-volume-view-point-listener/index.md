# VolumeViewPointListener | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform / VolumeViewPointListener 
# VolumeViewPointListener
```kotlin
interface VolumeViewPointListener
```
The  VolumeViewPointListener  interface. 
#### See also
Volume View Point Manager Members 
## Functions
on Viewpoint Changed 
```kotlin
abstract fun onViewpointChanged(viewpoint: ViewPoint)
```
Called when the viewpoint of the container changes.
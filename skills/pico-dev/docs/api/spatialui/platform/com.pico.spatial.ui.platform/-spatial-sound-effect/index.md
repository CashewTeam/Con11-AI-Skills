# SpatialSoundEffect | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform / SpatialSoundEffect 
# SpatialSoundEffect
```kotlin
enum SpatialSoundEffect : Enum<SpatialSoundEffect>
```
Spatial sound effects. Current supported effects in PICO OS 
Members Entries 
## Entries
OpClick 
```kotlin
OpClick
```
Operation click. 
OpDragBegin 
```kotlin
OpDragBegin
```
Operation drag begin. 
OpDragEnd 
```kotlin
OpDragEnd
```
Operation drag end. 
OpLongPress 
```kotlin
OpLongPress
```
Operation long press 
OpDragScale 
```kotlin
OpDragScale
```
Operation drag scale. 
StateSelected 
```kotlin
StateSelected
```
State selected. 
StateUnselected 
```kotlin
StateUnselected
```
State unselected. 
StateOn 
```kotlin
StateOn
```
State on. 
StateOff 
```kotlin
StateOff
```
State off. 
OpClose 
```kotlin
OpClose
```
Operation close. 
StateSuccess 
```kotlin
StateSuccess
```
State success. 
StateFailure 
```kotlin
StateFailure
```
State failure. 
## Properties
entries 
```kotlin
val entries: EnumEntries<SpatialSoundEffect>
```
Returns a representation of an immutable list of all enum entries, in the order they're declared. 
## Functions
value Of 
```kotlin
fun valueOf(value: String): SpatialSoundEffect
```
Returns the enum constant of this type with the specified name. The string must match exactly an identifier used to declare an enum constant in this type. (Extraneous whitespace characters are not permitted.) 
values 
```kotlin
fun values(): Array<SpatialSoundEffect>
```
Returns an array containing the constants of this enum type, in the order they're declared.
# equals | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform / ControllerHapticConfiguration / equals 
# equals
```kotlin
open operator override fun equals(other: Any?): Boolean
```
override equals function
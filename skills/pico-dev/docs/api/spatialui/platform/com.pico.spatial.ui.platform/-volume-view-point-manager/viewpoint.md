# viewpoint | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform / VolumeViewPointManager / viewpoint 
# viewpoint
```kotlin
abstract val viewpoint: State<ViewPoint>
```
The  State  of the current ViewPoint, default is  ViewPoint.Front . 
NOTE: Only the volumetric window's viewpoint may changes. 
#### See also
View Point
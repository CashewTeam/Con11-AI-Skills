# immersionMinLevel | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform / StageImmersionManager / immersionMinLevel 
# immersionMinLevel
```kotlin
abstract val immersionMinLevel: Int
```
The minimum immersion level of the Stage.
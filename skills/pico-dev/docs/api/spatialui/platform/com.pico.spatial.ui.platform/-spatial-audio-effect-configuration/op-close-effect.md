# opCloseEffect | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform / SpatialAudioEffectConfiguration / opCloseEffect 
# opCloseEffect
```kotlin
val opCloseEffect: SpatialSoundEffect
```
close sound effect in configuration
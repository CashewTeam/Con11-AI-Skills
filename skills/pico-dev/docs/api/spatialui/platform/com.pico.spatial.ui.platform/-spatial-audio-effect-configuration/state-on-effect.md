# stateOnEffect | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform / SpatialAudioEffectConfiguration / stateOnEffect 
# stateOnEffect
```kotlin
val stateOnEffect: SpatialSoundEffect
```
on state sound effect in configuration
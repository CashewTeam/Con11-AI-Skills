# playSystem | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform / SpatialAudioEffectPlayer / playSystem 
# playSystem
```kotlin
abstract fun playSystem(soundEffect: SpatialSoundEffect)
```
Plays the system sound effect.
# Companion | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform / ViewPoint / Companion 
# Companion
```kotlin
object Companion
```
Holds helpful values about  ViewPoint . 
Members 
## Properties
All 
```kotlin
val All: Set<ViewPoint>
```
A set contains all  ViewPoint s. 
Front Only 
```kotlin
val FrontOnly: Set<ViewPoint>
```
A set only contains front
# onCreate | PICO Spatial SDK

ui:platform / com.pico.spatial.ui.platform.stub / SpatialStubActivity / onCreate 
# onCreate
```kotlin
@CallSuper
```protected  open  override  fun  onCreate ( savedInstanceState :  Bundle ? )
# TabBarDefaults | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design.windows / TabBarDefaults 
# TabBarDefaults
```kotlin
object TabBarDefaults
```
TabBarDefaults contains the default values used by TabBar. 
Members 
## Properties
Expanded Alpha Hide Delay Millis 
```kotlin
const val ExpandedAlphaHideDelayMillis: Int = 800
```
The delay before expanded content starts fading out. 
Expanded Alpha Show Delay Millis 
```kotlin
const val ExpandedAlphaShowDelayMillis: Int = 1200
```
The delay before expanded content starts fading in. 
Expanded Content Height 
```kotlin
val ExpandedContentHeight: Dp
```
The fixed height of the expanded content area below the main tab row. 
Hover Delay Millis 
```kotlin
val HoverDelayMillis: Int
```
The delay when spatial hover effect. 
Item Corner Radius 
```kotlin
val ItemCornerRadius: Dp
```
The corner radius of the item. 
Item Expanded Horizontal Padding 
```kotlin
val ItemExpandedHorizontalPadding: Dp
```
The horizontal padding of the item when its expanded width exceeds  ItemMinWidth . 
Item Expand Height 
```kotlin
val ItemExpandHeight: Dp
```
The height of an item in the TabBar when both main and support content are present. 
Item Horizontal Padding 
```kotlin
val ItemHorizontalPadding: Dp
```
The horizontal padding of the item. 
Item Min Width 
```kotlin
val ItemMinWidth: Dp
```
The minimum width of an item in the TabBar. 
Item Normal Height 
```kotlin
val ItemNormalHeight: Dp
```
The height of an item in the TabBar. 
Items Count Limit 
```kotlin
const val ItemsCountLimit: Int = 7
```
The maximum count of items in a TabBar. 
Item Space 
```kotlin
val ItemSpace: Dp
```
The space between items in the TabBar. 
Item Text Height 
```kotlin
val ItemTextHeight: Dp
```
The height of the text area in an item. 
Item Vertical Padding 
```kotlin
val ItemVerticalPadding: Dp
```
The vertical padding of the support content in an item. 
Main Content Background Corner Radius 
```kotlin
val MainContentBackgroundCornerRadius: Dp
```
The corner radius of the main content background layer. 
Main Content Min Size 
```kotlin
val MainContentMinSize: Dp
```
The minimum size of the main content in an item. 
Main Expand Height 
```kotlin
val MainExpandHeight: Dp
```
The height of the TabBar when both main and support content are present. 
Only Main Height 
```kotlin
val OnlyMainHeight: Dp
```
The height of the TabBar when only main content is present. 
Tab Bar Container Bg Color 
```kotlin
val TabBarContainerBgColor: Color
```
The background color of the TabBar container. 
Tab Bar Corner Radius 
```kotlin
val TabBarCornerRadius: Dp
```
The corner radius of the TabBar container. 
Tab Bar Padding 
```kotlin
val TabBarPadding: Dp
```
The padding of the TabBar container. 
Tab Bar Top Offset 
```kotlin
val TabBarTopOffset: Dp
```
The offset of the TabBar from the top.
# Default | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design.windows / SubwindowPlacement / Default 
# Default
```kotlin
Default
```
Determined by the current layout direction. Left if layout direction is LTR, right if layout direction is RTL.
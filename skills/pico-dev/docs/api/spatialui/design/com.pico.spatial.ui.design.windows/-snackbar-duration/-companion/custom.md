# custom | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design.windows / SnackbarDuration / Companion / custom 
# custom
```kotlin
@Stable
```fun  custom ( durationMs :  Long ) :  SnackbarDuration 
Creates a custom duration for a snack. 
#### Return
A SnackDuration instance with the specified duration. 
#### Parameters
duration Ms 
The duration in milliseconds.
# alignTop | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design.windows.popup / VerticalPlacement / Companion / alignTop 
# alignTop
```kotlin
fun alignTop(): VerticalPlacement
```
Position the menu align the top of the anchor. 
#### Return
AlignTop 
```kotlin
fun alignTop(offset: Dp): VerticalPlacement
```
Position the menu align the top of the anchor. 
#### Return
AlignTop 
#### Parameters
offset 
The offset from the original aligned position of the popup.
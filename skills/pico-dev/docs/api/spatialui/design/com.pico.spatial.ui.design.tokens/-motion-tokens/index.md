# MotionTokens | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design.tokens / MotionTokens 
# MotionTokens
```kotlin
object MotionTokens
```
PICO Design's motion tokens. 
contains Spring & Bezier 
see ALIAS:REF_DF283661ED 
Members 
## Properties
bezier Easing Accelerate 
```kotlin
val bezierEasingAccelerate: Easing
```
A built-in bezier  Easing 
bezier Easing Decelerate 
```kotlin
val bezierEasingDecelerate: Easing
```
A built-in bezier  Easing 
bezier Easing Power Accelerate 
```kotlin
val bezierEasingPowerAccelerate: Easing
```
A built-in bezier  Easing 
bezier Easing Power Decelerate 
```kotlin
val bezierEasingPowerDecelerate: Easing
```
A built-in bezier  Easing 
bezier Easing Power Standard 
```kotlin
val bezierEasingPowerStandard: Easing
```
A built-in bezier  Easing 
bezier Easing Standard 
```kotlin
val bezierEasingStandard: Easing
```
A built-in bezier  Easing 
duration Long1 
```kotlin
val durationLong1: Int
```
A built-in duration token 
duration Long2 
```kotlin
val durationLong2: Int
```
A built-in duration token 
duration Long3 
```kotlin
val durationLong3: Int
```
A built-in duration token 
duration Long4 
```kotlin
val durationLong4: Int
```
A built-in duration token 
duration Medium1 
```kotlin
val durationMedium1: Int
```
A built-in duration token 
duration Medium2 
```kotlin
val durationMedium2: Int
```
A built-in duration token 
duration Medium3 
```kotlin
val durationMedium3: Int
```
A built-in duration token 
duration Medium4 
```kotlin
val durationMedium4: Int
```
A built-in duration token 
duration Short1 
```kotlin
val durationShort1: Int
```
A built-in duration token 
duration Short2 
```kotlin
val durationShort2: Int
```
A built-in duration token 
duration Short3 
```kotlin
val durationShort3: Int
```
A built-in duration token 
spring Easing Bouncy 
```kotlin
val springEasingBouncy: SpringEasingArgs
```
A built-in spring spec, see  toSpring 
spring Easing Fair 
```kotlin
val springEasingFair: SpringEasingArgs
```
A built-in spring spec, see  toSpring 
spring Easing Gradual 
```kotlin
val springEasingGradual: SpringEasingArgs
```
A built-in spring spec, see  toSpring 
spring Easing Instant 
```kotlin
val springEasingInstant: SpringEasingArgs
```
A built-in spring spec, see  toSpring 
spring Easing Rapid 
```kotlin
val springEasingRapid: SpringEasingArgs
```
A built-in spring spec, see  toSpring 
spring Easing Snapy 
```kotlin
val springEasingSnapy: SpringEasingArgs
```
A built-in spring spec, see  toSpring 
spring Easing Steady 
```kotlin
val springEasingSteady: SpringEasingArgs
```
A built-in spring spec, see  toSpring
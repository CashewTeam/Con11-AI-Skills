# badgeSize | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design / BadgeDefaults / badgeSize 
# badgeSize
```kotlin
fun badgeSize(width: Dp = Dp.Unspecified, height: Dp = Dp.Unspecified): BadgeSize
```
Create a  BadgeSize  with expected width and height. 
#### Return
The  BadgeSize  with expected width and height.
# systemColorScheme | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design / systemColorScheme 
# systemColorScheme
```kotlin
fun systemColorScheme(context: Context): ColorScheme
```
The colors will be load from system resources. 
#### Return
Returns a default PICO design  ColorScheme .
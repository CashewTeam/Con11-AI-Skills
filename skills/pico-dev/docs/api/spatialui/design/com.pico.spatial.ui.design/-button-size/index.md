# ButtonSize | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design / ButtonSize 
# ButtonSize
```kotlin
@Stable
```class  ButtonSize 
Presents sizes used by  Button . Holds pre-defined sizes used by styled buttons like  Button . Each size has related  PaddingValues , Shape  and  TextStyle . 
#### See also
Button Defaults. padding For Button Size Button Defaults. shape For Button Size Button Defaults. text Style For Button Members 
## Functions
copy 
```kotlin
fun copy(width: Dp = this.width, height: Dp = this.height, minWidth: Dp = this.minWidth, maxWidth: Dp = this.maxWidth, minHeight: Dp = this.minHeight, maxHeight: Dp = this.maxHeight): ButtonSize
```equals 
```kotlin
open operator override fun equals(other: Any?): Boolean
```hash Code 
```kotlin
open override fun hashCode(): Int
```
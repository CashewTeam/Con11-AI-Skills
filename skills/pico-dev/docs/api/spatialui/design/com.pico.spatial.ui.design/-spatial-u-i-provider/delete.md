# delete | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design / SpatialUIProvider / delete 
# delete
```kotlin
open override fun delete(uri: Uri, selection: String?, selectionArgs: Array<out String>?): Int
```
# copy | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design / PageControlSpec / copy 
# copy
```kotlin
fun copy(dotRadius: Dp = this.dotRadius, dotSpace: Dp = this.dotSpace, verticalPadding: Dp = this.verticalPadding): PageControlSpec
```
#### Return
a new  PageControlSpec  with the given parameters.
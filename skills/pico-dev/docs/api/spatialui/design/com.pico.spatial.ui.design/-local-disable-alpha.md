# LocalDisableAlpha | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design / LocalDisableAlpha 
# LocalDisableAlpha
```kotlin
val LocalDisableAlpha: ProvidableCompositionLocal<Float>
```
CompositionLocal containing the alpha value for component's disabled state 
Will be applied to component though Modifier.alpha
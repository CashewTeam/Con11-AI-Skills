# chipSize | PICO Spatial SDK

ui:design / com.pico.spatial.ui.design / ChipsDefaults / chipSize 
# chipSize
```kotlin
fun chipSize(height: Dp = Dp.Unspecified): ChipSize
```
Create a new  ChipSize  instance with expected height. 
#### Return
The new  ChipSize  instance with the given property
# defaultValue | PICO Spatial SDK

ui:foundation / com.pico.spatial.ui.foundation.dsl / Immersion / defaultValue 
# defaultValue
```kotlin
val defaultValue: Int
```
#### Parameters
default Value 
The default level of  Stage 's progressive in 0...100
# isDownEvent | PICO Spatial SDK

ui:foundation / com.pico.spatial.ui.foundation.gesture / SpatialPointerInfo / isDownEvent 
# isDownEvent
```kotlin
fun isDownEvent(): Boolean
```
For example,  MotionEvent.ACTION_DOWN  or  MotionEvent.ACTION_POINTER_DOWN 
#### Return
true  if this is a pointer down event
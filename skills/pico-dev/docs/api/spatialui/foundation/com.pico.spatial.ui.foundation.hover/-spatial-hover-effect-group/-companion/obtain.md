# obtain | PICO Spatial SDK

ui:foundation / com.pico.spatial.ui.foundation.hover / SpatialHoverEffectGroup / Companion / obtain 
# obtain
```kotlin
fun obtain(): SpatialHoverEffectGroup
```
Creates and returns a new SpatialHoverEffectGroup instance with an auto-incremented group ID. 
This method is thread-safe and generates unique IDs across all instances. 
#### Return
A new SpatialHoverEffectGroup instance with a unique ID
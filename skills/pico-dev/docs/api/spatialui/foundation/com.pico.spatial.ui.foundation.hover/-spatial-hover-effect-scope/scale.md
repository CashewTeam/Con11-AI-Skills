# scale | PICO Spatial SDK

ui:foundation / com.pico.spatial.ui.foundation.hover / SpatialHoverEffectScope / scale 
# scale
```kotlin
abstract fun scale(scale: Float, origin: TransformOrigin = TransformOrigin.Center)
```
Scale the view by given  scale  and  origin 
#### Parameters
scale 
the scale of view 
origin 
the origin of scale 
```kotlin
abstract fun scale(scaleX: Float, scaleY: Float, origin: TransformOrigin = TransformOrigin.Center)
```
Scale the view by given  scaleX  and  scaleY  and  origin 
#### Parameters
scale X 
the scale of view on x-axis 
scale Y 
the scale of view on y-axis 
origin 
the origin of scale
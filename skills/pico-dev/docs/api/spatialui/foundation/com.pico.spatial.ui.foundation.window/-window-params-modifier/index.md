# WindowParamsModifier | PICO Spatial SDK

ui:foundation / com.pico.spatial.ui.foundation.window / WindowParamsModifier 
# WindowParamsModifier
```kotlin
typealias WindowParamsModifier = (attributes: WindowManager.LayoutParams, triggerView: View) -> Unit
```
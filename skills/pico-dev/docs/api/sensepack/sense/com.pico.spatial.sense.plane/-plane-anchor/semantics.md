# semantics | PICO Spatial SDK

sense / com.pico.spatial.sense.plane / PlaneAnchor / semantics 
# semantics
```kotlin
val semantics: SemanticLabelType
```
The semantic data associated with the plane anchor. 
This provides additional information about the type or purpose of the mesh, which can be used for categorization or processing within the application.
# start | PICO Spatial SDK

sense / com.pico.spatial.sense.keyboard / PICOKeyboardTrackingManager / start 
# start
```kotlin
fun start()
```
Starts the PICO keyboard tracking manager. 
After this call succeeds, the manager can begin discovering keyboard anchors, serving  loadAllAnchors  requests, and producing live updates for registered subscribers.
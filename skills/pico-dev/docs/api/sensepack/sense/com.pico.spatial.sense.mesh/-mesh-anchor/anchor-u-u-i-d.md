# anchorUUID | PICO Spatial SDK

sense / com.pico.spatial.sense.mesh / MeshAnchor / anchorUUID 
# anchorUUID
```kotlin
val anchorUUID: UUID
```
The UUID ensures that each anchor is uniquely identifiable within the application.
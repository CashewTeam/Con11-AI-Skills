# vertices | PICO Spatial SDK

sense / com.pico.spatial.sense.mesh / MeshAnchor / vertices 
# vertices
```kotlin
val vertices: List<Vector3>
```
A list of vertices that define the geometry of the mesh anchor. 
Each vertex is represented as a 3D point in space (Vector3).
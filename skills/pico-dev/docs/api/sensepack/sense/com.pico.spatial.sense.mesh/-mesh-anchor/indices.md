# indices | PICO Spatial SDK

sense / com.pico.spatial.sense.mesh / MeshAnchor / indices 
# indices
```kotlin
val indices: List<Int>
```
A list of indices that define the connectivity of the vertices in the mesh. 
These indices are used to define the faces or triangles of the mesh.
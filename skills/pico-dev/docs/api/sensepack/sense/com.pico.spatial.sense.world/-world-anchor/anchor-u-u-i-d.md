# anchorUUID | PICO Spatial SDK

sense / com.pico.spatial.sense.world / WorldAnchor / anchorUUID 
# anchorUUID
```kotlin
val anchorUUID: UUID
```
The UUID of the anchor, which ensures that each anchor is unique and identifiable within the application.
# build | PICO Spatial SDK

tracking / com.pico.spatial.tracking.body / BodyTrackingStartInfo / Builder / build 
# build
```kotlin
fun build(): BodyTrackingStartInfo
```
Builds a  BodyTrackingStartInfo  instance with the configured settings. 
#### Return
A new  BodyTrackingStartInfo  instance.
# addListener | PICO Spatial SDK

tracking / com.pico.spatial.tracking / DataProvider / addListener 
# addListener
```kotlin
abstract fun addListener(listener: DataProvider.DataListener<T>)
```
Adds a  DataListener  to receive data. 
#### Parameters
listener 
The  DataListener  to add.
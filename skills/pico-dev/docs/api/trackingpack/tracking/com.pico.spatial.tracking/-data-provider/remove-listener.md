# removeListener | PICO Spatial SDK

tracking / com.pico.spatial.tracking / DataProvider / removeListener 
# removeListener
```kotlin
abstract fun removeListener(listener: DataProvider.DataListener<T>)
```
Removes a  DataListener  to stop receiving data. 
#### Parameters
listener 
The  DataListener  to remove.
# dataFlow | PICO Spatial SDK

tracking / com.pico.spatial.tracking / DataProvider / dataFlow 
# dataFlow
```kotlin
abstract val dataFlow: SharedFlow<T>
```
The  SharedFlow  of tracking data. This flow is a hot flow with no replay.
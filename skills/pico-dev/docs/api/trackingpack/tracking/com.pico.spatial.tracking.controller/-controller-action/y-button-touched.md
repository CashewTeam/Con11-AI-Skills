# yButtonTouched | PICO Spatial SDK

tracking / com.pico.spatial.tracking.controller / ControllerAction / yButtonTouched 
# yButtonTouched
```kotlin
val yButtonTouched: Boolean
```
Whether the Y button (on left controller) is touched.
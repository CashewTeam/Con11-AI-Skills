# aButtonTouched | PICO Spatial SDK

tracking / com.pico.spatial.tracking.controller / ControllerAction / aButtonTouched 
# aButtonTouched
```kotlin
val aButtonTouched: Boolean
```
Whether the A button (on right controller) is touched.
# xButtonTouched | PICO Spatial SDK

tracking / com.pico.spatial.tracking.controller / ControllerAction / xButtonTouched 
# xButtonTouched
```kotlin
val xButtonTouched: Boolean
```
Whether the X button (on left controller) is touched.
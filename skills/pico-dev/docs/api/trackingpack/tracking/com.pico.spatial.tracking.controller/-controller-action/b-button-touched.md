# bButtonTouched | PICO Spatial SDK

tracking / com.pico.spatial.tracking.controller / ControllerAction / bButtonTouched 
# bButtonTouched
```kotlin
val bButtonTouched: Boolean
```
Whether the B button (on right controller) is touched.
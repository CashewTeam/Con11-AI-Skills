将法线向量从对象空间或切线空间转换为世界空间。

## **参数说明**

* **In**：要转换的输入向量；默认值为 `(0.5, 0.5, 1.0)`。
* **Space**：指定法线向量的来源空间，可选值为 **object** 或 **tangent**；默认值为 **tangent**。
* **Scale**：在转换前施加到输入向量上的缩放系数；默认值为 **1.0**。
* **Normal**：表面法线向量；默认值为当前世界空间中的表面法线。
* **Tangent**：表面切线向量；默认值为当前世界空间中的切线向量。


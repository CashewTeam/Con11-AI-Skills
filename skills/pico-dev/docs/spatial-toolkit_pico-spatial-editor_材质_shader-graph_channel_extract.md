从 **Color N** 或 **Vector N** 数据流的单个通道中生成一个 Float 数据流。

## 参数说明

* **In**: 输入数据流。
* **Index**：索引。

## 节点使用说明
**Extract** 节点接收 **In** 端口的输入，并始终输出一个单独的 **Float** 类型的值。输出值是 **In** 中由 **Index** 指定位置的数值。例如，如果 **In** 是值为 `(10, 15, 20)` 的 **Vector 3**，且 **Index** 为 `1`，则输出为 `15`。**Index** 的默认值为 `0`。


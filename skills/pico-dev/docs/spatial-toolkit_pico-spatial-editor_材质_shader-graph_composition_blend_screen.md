一种混合操作，会提亮比白色更暗的区域。
该节点会先对前景和背景的颜色值分别取反，再将这些结果相乘，最后再次对结果取反。其计算公式为：`1 − (1 − F)(1 − B)`。

## **参数说明**

* **Foreground**：前景输入，在公式中记作 `F`。
* **Background**：背景输入，在公式中记作 `B`。
* **Mix**：混合操作的权重。**Mix** 值越高，混合效果越明显。默认值为 `1`。取值超出 `0` 到 `1` 范围时，会产生超出该节点预期功能之外的未定义效果。

### **节点使用说明**
这种混合方式产生的视觉效果，总是与原始纹理一样亮，或比原始纹理更亮。
下面的示例节点图展示了如何使用 **Screen** 节点将两张图像混合为一个材质。

下方展示了两张原始图像，以及将混合后的纹理应用到立方体表面的效果（**Mix** = 0.5）。

<strong>Foreground</strong>

<strong>Background</strong>

混合后的材质如下：


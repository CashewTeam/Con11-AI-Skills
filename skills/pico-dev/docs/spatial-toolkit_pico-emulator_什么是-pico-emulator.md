PICO Emulator 是一款能在 PC 端模拟 PICO OS 6 环境的工具。你可以在 PICO Emulator 中调试空间应用。
PICO Emulator 支持以下功能：

* 安装被打包为 .apk 文件的空间应用。
* 向已安装的空间应用推送 .obb 文件或 .bundle 文件。
* 模拟眼手交互或手柄交互。
* 通过手柄按键模式把 PC 键盘映射为相应的手柄按键。
* 支持 UI 调试，包括性能数据模式、包围盒模式、碰撞体模式和坐标轴模式。
* 支持 MR 模拟功能，包括视频透视模拟和空间网格模拟。


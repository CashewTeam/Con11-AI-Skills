将前景值从背景值中相减。
**Subtractive Mix** 节点对两个输入执行相减操作，并使用 **Mix** 输入来决定混合中前景的权重，其公式表示为：`B − F`。

## **参数说明**

* **Foreground**：前景输入，在公式中记作 `F`。
* **Background**：背景输入，在公式中记作 `B`。
* **Mix**：混合效果的权重。**Mix** 值越高，混合操作的强度越大，视觉效果也越明显。默认值为 `1`。取值超出 `0` 到 `1` 范围时，会产生超出该节点预期功能之外的未定义效果。

## **节点使用说明**
下面是一个简单的节点图示例，展示了如何使用 **Subtractive Mix** 节点将两张图像混合为一个材质。

下图展示了两张原始图像和混合后的材质（**Mix**  = 1）。

<strong>Foreground</strong>

<strong>Background</strong>

混合后的材质如下：


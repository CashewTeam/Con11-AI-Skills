返回对象前方向在指定空间中的 float3 方向向量。

## 参数说明

* **Space**：返回的向上向量所使用的坐标空间。默认为 **world**。**Space** 可被设置为以下值。
   * **object**：局部坐标空间。在着色器对几何体应用局部形变之后、但应用全局变换之前的坐标空间。
   * **view**：相机空间。在着色器对几何体应用局部形变和全局变换之后，再转换到以当前相机为参考的坐标空间。在该空间中，方向和位置是相对于相机来表示的。
   * **world**：全局坐标空间。在着色器对几何体同时应用局部形变和全局变换之后的坐标空间。


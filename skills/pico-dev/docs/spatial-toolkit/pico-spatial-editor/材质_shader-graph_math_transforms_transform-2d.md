对 2D 输入应用仿射变换的节点。

## 参数说明

* **In**：要进行变换的向量。该节点会在输入向量 **In** 后附加一个值为 **1.0** 的额外分量，使该向量的维度与 2D 仿射变换矩阵的维度匹配。变换完成后，这个额外分量会被移除。
* **Rotation**：要应用到输入上的逆时针旋转角度，单位为度。
* **Scale**：要应用到输入上的缩放。此参数会按给定的倍率对输入进行拉伸或缩放。
* **Translation**：要应用到输入上的平移。平移会使输入沿给定方向移动。


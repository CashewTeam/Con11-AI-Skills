将一个颜色从 HSV 颜色空间转换回 RGB 颜色空间。

## 参数说明

* **In**: 输入一个包含色相 (Hue)、饱和度 (Saturation) 和明度 (Value) 的颜色向量。
* **Out**: 输出一个标准的 RGB 格式颜色。

## 节点使用说明
此节点是 **RGB to HSV** 节点的逆操作，它执行颜色空间转换算法，将在 HSV 空间下调整好的颜色重新映射回设备屏幕能够显示的 RGB 格式。


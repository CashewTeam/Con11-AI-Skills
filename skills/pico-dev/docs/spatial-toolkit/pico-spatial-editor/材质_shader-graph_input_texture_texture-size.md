获取纹理大小和纹素大小。

## 参数说明

* **Filename**：用作纹理的图像文件。

## 输出说明

* **Width**：纹理的宽度（像素）。
* **Height**：纹理的高度（像素）。
* **TexelWidth**：单个纹素在 U 方向对应的纹理坐标大小，值为 1/**Width**。
* **TexelHeight**：单个纹素在 V 方向对应的纹理坐标大小，值为 1/**Height**。

